import React from "react";
import ReactDOM from "react-dom";
import ResumeGenerator from "./App";

ReactDOM.render(
  <React.StrictMode>
    <ResumeGenerator />
  </React.StrictMode>,
  document.getElementById("root")
);
